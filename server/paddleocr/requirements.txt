# Pinned: paddleocr 3.x result keys (rec_texts/rec_scores/rec_boxes) are what
# server.py reads — bump these pins together and re-test /ocr before upgrading.
#
# paddlex must be pinned too: paddleocr declares "paddlex>=3.1.0" with no
# upper bound, so an unpinned install resolves the latest paddlex. paddlex's
# PaddlePredictorOption API changed since 3.1.0 (it dropped the positional
# model_name argument paddleocr 3.1.0 still passes), which fails with
# "PaddlePredictorOption.__init__() takes 1 positional argument but 2 were
# given" at PaddleOCR(...) construction time. Pin paddlex to the version
# contemporaneous with paddleocr 3.1.0 to avoid this.
paddlepaddle==3.0.0
paddleocr==3.1.0
paddlex==3.1.0
flask==3.0.3
pillow>=10
numpy
